
// var addres = Address(numero: 'numero', rua: 'rua', bairro: 'bairro');

main() {
  // final fake = FakeFirebaseFirestore();

  // OrderRepository repository = OrderRepository(fake);

  // group('Store', () {
  //   test('Orders', () {
  //     final s = repository.openOrders();

  //     expect(
  //         s,
  //         emitsInOrder([
  //           [],[order.toMap()],
  //         ]));
  //     repository.requestMotoboy(order);
  //     // repository.openOrders();
  //   });
  // });
}
